# Heap Binario de Maximos
Implementacion simple de un Heap Binario de maximos en Lua.

Incluye el modulo de lua con todas las funciones (heap.lua) y un ejemplo de uso y funcionalidad correcta (ejemplo.lua)
